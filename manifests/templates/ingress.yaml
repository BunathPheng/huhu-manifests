---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: huhu                  # Ingress name
  labels:
    app: huhu
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "huhu.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: huhu               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
